import css from 'styled-jsx/css'
import { theme } from 'theme'

export default css`
    main{
        height: 100vh;
        max-width: 100%;
        background-color: ${theme.backgroundColor.warn};
    }
`